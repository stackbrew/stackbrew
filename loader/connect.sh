#!/bin/bash


function main() {
	
	# Read request from stdin

	# Execute connector
	CONNECTOR_SCRIPT="$(cue export --out text $CONNECTOR -e '')"
	bash -x "$(cue 

	# Write response to stdout
}

func main() {
	evalScript <<-EOF
		case '\(task.action)' in
			mkdir)
				if [ '\(task.
			;;

		esac
		\(["k=v" for k, v in task.environment]
	EOF
}

